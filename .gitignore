# Build output
/dist/
*.exe
*.test
*.out
*.prof

# Local dev
.env
*.local

# Never commit decrypted secrets or a live secrets dir
*.sops.decrypted
/run/
/secrets/
/spool/

# Internal governance and context docs live on disk here but are versioned
# centrally in the projects-docs repo, never in this public repo.
/CLAUDE.md
/CHARTER.md
# docs/ stays public because users need it: README, LABELS.md, INGEST.md,
# docs/TESTING.md (the Testing Standard mandates and CI-gates it), SECURITY.md,
# design docs. The one internal carve-out is the raw decisions log, versioned
# centrally in projects-docs and never published here.
/docs/DECISIONS.md
